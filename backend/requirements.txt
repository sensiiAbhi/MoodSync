# MoodSync Backend Dependencies
# Python 3.11

# ── Web Framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Database ──
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# ── Auth & Security ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── Validation ──
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# ── HTTP Client ──
httpx==0.27.2

# ── ML & Data Science ──
# Use pre-built wheels only — no compilation needed on Render
numpy==1.26.4
scikit-learn==1.5.2
# scipy intentionally omitted — scikit-learn installs a compatible version automatically

# ── Caching (optional) ──
redis==5.0.8

# ── Utilities ──
python-dateutil==2.9.0
pytz==2024.2
